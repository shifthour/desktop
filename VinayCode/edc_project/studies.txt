as you know that we are building  (EDC) system for clinical trials. The Form Builder module is already implemented. Now, design and generate the Studies Tab module that will act as the main container for forms, subjects, visits, and queries.

Requirements:

Studies List View

Show all studies with columns: Study ID, Study Name, Phase, Status (Start-up / Conduct / Close-out), Created By, Last Modified.

Actions: Create New Study, Edit Study (only in Start-up phase), View Study.

Study Dashboard (when a study is selected)

High-level metrics: #Subjects enrolled, #Forms completed, #Open queries, Visit completion %.

Tabs inside the study:

Subjects Tab (list of subjects, add new subject, subject status).

Forms Tab (show visit schedule + linked forms from Form Builder).

Queries Tab (auto + manual queries, workflow statuses).

Audit Log Tab (show actions: form release, data entry, query raised/resolved).

Permissions

Study Designer: can create/edit studies and assign forms during Start-up.

CRC/PI: can see study and perform data entry once in Conduct.

DM/CRA: can view study, raise queries, monitor data quality.

POC Demo Flow

User opens Studies Tab → selects a study → sees dashboard → navigates to Subjects → adds subject → enters Demographic form (already built) → validation triggers → data saved → query raised → query shown in Queries Tab.

Output Format

Provide a React component with Tailwind CSS (or equivalent UI framework).

Ensure the design looks modern and clean.

Use placeholders/dummy data for now, but structure it so real data can be integrated later.
as you know that we are building an Electronic Data Capture (EDC) system for clinical trials. 
We already have Form Builder, Studies tab, Subjects tab, Queries tab, and Audit Trail tab. 
Now we want to create a combined "Dashboards & Reports" tab to provide study insights and exportable reports.

Requirements:

1. Dashboards Section
   - Show visual charts and KPIs for:
     a) Study Metrics:
        - Subjects by status (Screened, Enrolled, Completed, Withdrawn)
        - Visit completion rates (planned vs completed visits)
        - Site-level progress (enrollment per site)
     b) Data Quality:
        - Open queries (by site, by severity)
        - Query aging (0–7 days, 8–14 days, >14 days)
        - Validation rule failure counts
     c) Compliance:
        - Database lock/unlock status
        - Pending e-signatures (PI approvals)
        - User activity summary
   - Use charts (bar, pie, line) and KPI cards for visualization.
   - Clicking on a chart should allow drill-down into detailed data.

2. Reports Section
   - Provide tabular, exportable reports such as:
     a) Subject enrollment log
     b) Query status report (open/closed/answered by site)
     c) Audit trail summary
     d) Visit completion report
   - Each report should support:
     - Filters (by study, site, subject, date range, status)
     - Sorting and search
     - Export options (CSV, XLSX, PDF)

3. UI/UX
   - The tab should have two main sections: 
     a) "Dashboards" (visual view with charts and KPIs)
     b) "Reports" (detailed tables with export options)
   - Clear n

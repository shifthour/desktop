My requirements:

Im changing the status of a record from UI selenium automation with java 11. 
The same record is to be reverted after certain validations and this revert is done through API call. We now need a java class for this. I already have browserconfig class in my framework and the type of browser to be used is configured in config.properties. The current API call should support any kind of browser.

Sample config.properties file # --- STATUS MAPPING ---
S1=OPEN
S2=CLOSED
S3=EARLYCLOSE
S4=HOLD

# --- REASON MAPPING ---
R1=POWROUT
R2=MAINTENANCE
R3=NETWORK
R4=TEST

# --- COMMENT MAPPING (optional codes) ---
C1=AUTOMATED_REVERT
C2=MANUAL_OVERRIDE

# --- API CONFIG ---
base.url=https://your-api-host/api/v1/revert
revert.endpoint=/revertStatus

----------------------------------
I have something like this in feature file
 Then I click on save button
Then I verify the changes
And I revert the status if bcids edited
Then i verify the response code.

In my stepdefinition file, I'm calling the below sequence for the feature line I click on save button
RevertStatus.enableNetworkCapture(driver);
editRecord.clickSaveButton();
RevertStatus.disableNetworkCapture();

because we need not start capturing the network from the beginning. Only after clicking on save button our required service will be hit.

No need to set the thread count value. Anyways the number of records edited will not be more than 2 at a time. Give the code for reason, date, comments also. While building revertjson payload, include status, comments, reason, date also. If im passing null, let it be null else update the values. Reason will be R1, R2 etc. in feature file i give reason as power outage but in query i have to update as POWROUT as its database specific code. So give such mapping for all in apiconfig.properties the same applies to status too. Keep all the field updates in same method while building json.All the fields are passed in feature file as they are mandatory during initial UI update. The revert status has only few fields to update and sometimes many based on the initial and final status. Example closed to open will have only 2 field to update like status and comments. But open to closed will have reason, date , comments, status to update. 